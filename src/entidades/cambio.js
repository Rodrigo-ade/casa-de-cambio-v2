export class Cambio{
  constructor(divisaBase,divisaObjetivo,monto,montoResultado){
    this.divisaBase = divisaBase;
    this.divisaObjetivo = divisaObjetivo;
    this.monto = monto;
    this.montoResultado = montoResultado;
  }
};
